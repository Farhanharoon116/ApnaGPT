{
  "name": "apnagpt",
  "version": "1.0.0",
  "description": "ApnaGPT - Personal ChatGPT-style AI chat interface powered by Groq & SQLite",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue,green\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client && npm run db:migrate",
    "db:migrate": "npm run prisma:migrate --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": ["groq", "chatgpt", "ai", "react", "tailwindcss", "express", "sqlite", "prisma", "llm", "llama-3"]
}
